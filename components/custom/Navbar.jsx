import React from 'react';
import { HeroShareApp } from '../hero-share-app';

const Navbar = () => {
    return (
        <div>
            <HeroShareApp/>
        </div>
    );
};

export default Navbar;